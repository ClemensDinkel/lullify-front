const formList = [
  {
    name: "title",
    description: "Video Title",
    type: "text",
    required: true,
    maxlength: "40",
    tooltip: "Video Title should have maximum 40 characters.",
    placeholder: "Enter Title"
  }
]

/* let video = new Object();
video[formlist[0].name] = "sünder"
console.log(video.title) */

export default formList